galaxy_info:
  author: yoshiaki tanaka
  description: install java platform
  license: MIT
  min_ansible_version: 2.4
  platforms:
  - name: Debian
    versions:
      - stretch
  galaxy_tags: []

dependencies:
  - role: apt-backports
    when: java_version > 8
